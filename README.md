# Power-Admin
Créditos: Infinite yield

# library:
add comando:
```lua
CMDs[#CMDs + 1] = {NAME = 'teste', DESC = 'teste'}
```

add cmd:
```lua
addcmd('teste',{},function(args, speaker)
	-- códigos
end)
```

notificação:
```lua
notify('teste','teste')
```
